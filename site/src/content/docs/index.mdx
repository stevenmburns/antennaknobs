---
title: AntennaKNoBs
description: A browser-based wire-antenna method-of-moments simulator. Turn a knob, watch the radiation pattern, SWR, and impedance move in real time.
template: splash
hero:
  title: AntennaKNoBs
  tagline: Turn a knob, watch the antenna. A browser-based wire-antenna MoM simulator — no install, no NEC deck to hand-write.
  actions:
    - text: Open the live simulator
      link: https://app.antennaknobs.dev/
      icon: external
      variant: primary
    - text: Download for Windows
      link: /start/workbench/
      icon: download
      variant: secondary
    - text: Quickstart
      link: /start/quickstart/
      icon: right-arrow
      variant: minimal
    - text: View on GitHub
      link: https://github.com/stevenmburns/antennaknobs
      icon: github
      variant: minimal
---

import { Card, CardGrid, LinkCard } from "@astrojs/starlight/components";

{/* What's-new box: refresh as part of the release ritual (bump the version,
    swap the highlights) — a stale "new" box is worse than none. */}

<Card title="New in v0.77.0" icon="star">
  **Where the deck puts it.** An imported deck's off-centre feeds, loads and
  NEC-5 knot sources now sit exactly where the deck puts them, on wires kept
  whole: each engine meshes the wire so the port lands on its own grid, and
  says so when it cannot. 4nec2's percentage positions (`EX 0 2 50%`) land
  exactly there too. A NEC-5 deck's discrete `LD` cards are read the way NEC-5
  means them, one load at a knot; a `CM NEC-5` card declares a hand-written
  deck NEC-5; and the NEC-2 export writes fixed-impedance loads. Both band
  pickers take a **Custom…** band, so a design can sit at any frequency, 300
  MHz included, and a `settings.toml` sets where the workbench starts: its
  switches, ground, solver slots and engine paths. Buried radial screens wider
  than four wavelengths in the soil now solve instead of refusing. In the
  workbench, charts keep their size after a trip to the grid layout, and the
  layout toggle no longer covers the 3D view's Iso button. Imported decks with
  off-centre attachments read slightly different impedances than before.
</Card>

## The whole pitch, in one line

antennaknobs describes an antenna **as Python code** — a small parametric
*builder*, its geometry computed from named knobs — then lets you explore it two
ways: drive it from a script (sweep, optimize, compare, export a NEC deck), or
open it in a browser and **turn the knobs live**, watching the radiation
pattern, SWR, and impedance respond as you drag. Most antenna tools are a static
formula calculator or a desktop app you have to install; this is neither.

## See it in action

A live modeling session, end to end: a 10&nbsp;m Moxon built from knobs, tuned
by dragging, and read out in real time.

<div style={{ position: "relative", paddingBottom: "56.25%", height: 0, overflow: "hidden", borderRadius: "0.5rem" }}>
  <iframe
    src="https://www.youtube-nocookie.com/embed/WmQ-2Kfz6dU"
    title="AntennaKNoBs by KK7KNB: Live Antenna Modeling in the Browser (10m Moxon Demo)"
    style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", border: 0 }}
    allow="accelerometer; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowFullScreen
  ></iframe>
</div>

And the other half of the story — writing an antenna *as code*: a full
tutorial building a parameterized single-wire dipole from an empty file to
knobs turning in the workbench.

<div style={{ position: "relative", paddingBottom: "56.25%", height: 0, overflow: "hidden", borderRadius: "0.5rem", marginTop: "1rem" }}>
  <iframe
    src="https://www.youtube-nocookie.com/embed/5lxzkic8pnA"
    title="First AntennaKNoBs Python Coding Tutorial for a Fully Parameterized Single-Wire Dipole"
    style={{ position: "absolute", top: 0, left: 0, width: "100%", height: "100%", border: 0 }}
    allow="accelerometer; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowFullScreen
  ></iframe>
</div>

<CardGrid stagger>
  <Card title="Antennas as Python code" icon="pencil">
    Describe an antenna once as a small parametric builder — geometry computed
    from named knobs — then sweep, optimize, compare, or open it in the browser.
    Most tools make you hand-place every coordinate.
  </Card>
  <Card title="Multi-basis MoM" icon="seti:python">
    Sinusoidal and B-spline bases in one engine, cross-validating each
    other — basis quality that otherwise costs four figures in commercial
    tools.
  </Card>
  <Card title="Fast on big arrays" icon="rocket">
    H-matrix / ACA acceleration with GMRES, where every free NEC tool is dense
    O(N²). Demonstrated O(N log N) on a 2142-segment Yagi.
  </Card>
  <Card title="Cross-checked physics" icon="approve-check">
    A NEC-2 reference engine runs alongside the in-house solver — and a
    licensed NEC-5 can join as a third — so you get a built-in accuracy
    check most tools lack.
  </Card>
  <Card title="No platform lock-in" icon="laptop">
    A web UI by default — works anywhere a browser does, while the competition
    is Windows/Linux/macOS desktop-only or a GUI-less library.
  </Card>
</CardGrid>

## Start here

<CardGrid>
  <LinkCard title="What is antennaknobs?" href="/start/welcome/" description="The tool, who it's for, and how the pieces fit." />
  <LinkCard title="Quickstart" href="/start/quickstart/" description="pip install, then a resonant dipole in ten lines of Python." />
  <LinkCard title="Many ways to express geometry" href="/concepts/authoring/" description="The same delta loop, one knob-set, built four different ways — pick the one that fits how you think." />
  <LinkCard title="The solver & accuracy" href="/reference/solver/" description="Bases, H-matrix acceleration, and how it's validated." />
</CardGrid>
